<!-- <%= link_to 'Back', @movie, class: "back-btn" %> -->

<%= javascript_pack_tag 'room' %>
<%= content_tag :div,
  id: "room_data",
  reservations: @initialState.to_json,
  data: @room.to_json do %>
<% end %>
